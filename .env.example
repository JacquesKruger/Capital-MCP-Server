# Capital.com API Environment Variables
# Copy this file to: secrets/.env.demo or secrets/.env.live
# Then fill in your actual credentials from Capital.com
# NEVER commit actual credentials to GitHub!

# API Environment: "demo" or "live"
CAP_ENVIRONMENT=demo

# API Credentials (get from Capital.com developer portal)
CAP_API_KEY=your_api_key_here
CAP_IDENTIFIER=your_identifier_here
CAP_PASSWORD=your_password_here

# Optional: Override API Base URLs (leave commented for defaults)
# CAP_DEMO_API_URL=https://demo-api-capital.backend-capital.com
# CAP_LIVE_API_URL=https://api-capital.backend-capital.com
